# conformance 门禁数据：已知差异白名单 + 全等段数基线。
#
#   baseline=N              诊断对照中「文本+位置全等」段数的下限（只增不减）
#   <段标识> <类别> <成因>   允许存在的差异；段标识即报告里的 `路径[段号]`
#
# 修复后须从本表删除对应条目——表中条目越少越好。新增差异不在表内时，
# `zig build conformance` 直接失败（报告仍会写出，便于定位）。
#
# 本表只收「有意义的暂缓」：功能点本身正当，但当前无法由通用规则实现——判据是
# 「换成同类另一输入，行为仍由同一套规则导出」，即不得做用例特判。四处差异的共同
# 性质是**恢复粒度**——参照实现
# 在报错后丢弃该 token 并由 LALR 状态重新同步，本库就地报错后继续按语法规则解析，
# 于是多出后续 token 的一条诊断（都与「多报」或报点行号有关，无漏报）。

baseline=100

# use 声明族恢复：`namespace Foo` 缺分号与连续 `use` 语句——参照实现丢弃报错的 token
# 后从下一行重新同步，本库在同一 token 处继续解析，报点行号与后续 break/throw 的
# 收尾检查不同（期望 5 条 / 实际 6 条）。
errorHandling/recovery.test[16]        count
# 闭包体缺失：`function(Foo);` 参数位报错后，参照实现对紧随的 `;` 静默，本库按语法
# 规则继续解析该 token 并报「缺体」（期望 7 条 / 实际 8 条）。曾以「本次解析已报错
# 则空块占位」的用例特判规避，已按通用性铁律回退。
errorHandling/recovery.test[21]        count
# heredoc flexible 缩进诊断：参照实现从缩进串起点报整段，本库从行首有效缩进处报，
# 触发点与区间不同（期望 8 条 / 实际 7 条，此处为漏 1 条 + 位置口径差异）。
scalar/flexibleDocStringErrors.test[0] count
# 数字 `_` 分隔后的残句：`1_.0;` 的 `_` 被切出后成为表达式残留，参照实现把它并入
# 前一语句的恢复（只报 1 条），本库按「identifier 可作新语句起点」的通用同步规则
# 继续解析，于是 `.0` 再报一条（期望 8 条 / 实际 9 条）。曾以「仅行首标识符才算
# 同步点」的用例特判规避，已按通用性铁律回退。
scalar/numberSeparators.test[0]        count
