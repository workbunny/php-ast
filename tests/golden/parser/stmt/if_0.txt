(root `<?php`
  (stmt_if `if`
    (expr_variable `$a`)
    (stmt_block `{`)
    (stmt_if `elseif`
      (expr_variable `$b`)
      (stmt_block `{`)
      (stmt_if `elseif`
        (expr_variable `$c`)
        (stmt_block `{`)
        (stmt_block `{`)
      )
    )
  )
  (stmt_if `if`
    (expr_variable `$a`)
    (stmt_block `{`)
  )
  (stmt_if `if`
    (expr_variable `$a`)
    (stmt_block `:`)
    (stmt_if `elseif`
      (expr_variable `$b`)
      (stmt_block `:`)
      (stmt_if `elseif`
        (expr_variable `$c`)
        (stmt_block `:`)
        (stmt_block `:`)
      )
    )
  )
  (stmt_if `if`
    (expr_variable `$a`)
    (stmt_block `:`)
  )
  (stmt_nop `// without else`)
)
