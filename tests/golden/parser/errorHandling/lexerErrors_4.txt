; 2 条诊断
;   Unterminated comment from 5:5 to 7:1
;   Syntax error, unexpected EOF from 7:1 to 7:1
(root `<?php`
  (stmt_if `if`
    (expr_variable `$b`)
    (stmt_block `{`
      (stmt_expression `=`
        (expr_assign `=`
          (expr_variable `$a`)
          (expr_int `1`)
        )
      )
      (stmt_nop `/* unterminated\n}\n`)
    )
  )
)
