; 1 条诊断
;   Syntax error, unexpected ')' from 4:8 to 4:8
(root `<?php`
  (stmt_expression `=`
    (expr_assign `=`
      (expr_variable `$i`)
      (expr_int `0`)
    )
  )
  (stmt_expression `=`
    (expr_assign `=`
      (expr_variable `$j`)
      (expr_int `1`)
    )
  )
  (stmt_expression `=`
    (expr_assign `=`
      (expr_variable `$k`)
      (expr_int `2`)
    )
  )
  (stmt_nop `// The output here drops the loop - would require Error node to handle this`)
)
